const { title, description, url, image, author } = Astro.props;
const subtitle = 'Accessible Astro Starter';

// General meta tags
const pageTitle = `${title} - ${subtitle}`;
const metaTags = {
  title: pageTitle,
  description: description,
  author: author,
};

// Open Graph tags
const ogTags = {
  title: pageTitle,
  description: description,
  type: 'website',
  url: url,
  image: Astro.site ? `${Astro.site}${image}` : image,
};

// Twitter Card tags (if needed)

// Page title
const pageTitleElement = <title>{pageTitle}</title>;

// Now you can use these variables in your HTML meta tags or template engine, e.g. React, JSX, etc.
